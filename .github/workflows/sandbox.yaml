name: sandbox

on:
  push:
  workflow_dispatch:
    inputs:
      vars:
        description: ''
        required: false
        default: 'a,b,c'

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
    - id: set-matrix
      env:
        VARS: ${{ github.event.inputs.vars }}
      run: |
        echo $VARS

        header='{"config":'
        content=''
        for t in $(echo $VARS | tr ',' ' '); do
          content+=$header
          content+="\"$t\"},"
        done
        content=${content%,}

        # echo "::set-output name=$(printf 'matrix::{\"include\":[%s]}' $content)"
        echo "::set-output name=matrix::{\"include\":[{\"config\":\"a\"},{\"config\":\"b\"},{\"config\":\"c\"}]}"
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
  job2:
    needs: job1
    runs-on: ubuntu-latest
    strategy:
      matrix: ${{ fromJson(needs.job1.outputs.matrix) }}
    steps:
    - name: debug
      env:
        context: ${{ toJson(matrix) }}
      run: |
        echo $context
